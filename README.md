Mapa Potencjału — Final Perfect ZIP (rich visual)
Instructions:
1. Unzip and upload ALL FILES/FOLDERS to the ROOT of your GitHub repository (index.html and package.json must be in root).
2. Connect repository to Vercel and Deploy.
Local test:
- npm install
- npm run dev
Notes:
- Uses relative asset paths (base: './') to avoid MIME issues on Vercel.
